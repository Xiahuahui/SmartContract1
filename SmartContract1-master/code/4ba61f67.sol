//BCMETH means Blockchain Match Ethereum 
pragma solidity ^0.4.24;

contract BCMETH {
    String currentStatus;
    constructor () public {
        currentStatus="[1, 1, 1]";
    }

    function (Term1A!judge(A,2)|Ajudge(B,3), Term1Ajudge(A,2)|A!judge(B,3), Term1Ajudge(A,2)|Ajudge(B,3), Term2Ajudge(A,2), Term3Ajudge(A,2))(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1]" && action=="(Term1A!judge(A,2)|Ajudge(B,3), Term1Ajudge(A,2)|A!judge(B,3), Term1Ajudge(A,2)|Ajudge(B,3), Term2Ajudge(A,2), Term3Ajudge(A,2))"){
            currentStatus="[2, 2, 2]";
            return true;
        }
        else
            return false;
    }

    function (Term1A!judge(A,2)|Ajudge(B,3), Term1Ajudge(A,2)|A!judge(B,3), Term1Ajudge(A,2)|Ajudge(B,3), Term2Ajudge(A,2), Term3A!judge(A,2))(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1]" && action=="(Term1A!judge(A,2)|Ajudge(B,3), Term1Ajudge(A,2)|A!judge(B,3), Term1Ajudge(A,2)|Ajudge(B,3), Term2Ajudge(A,2), Term3Ajudge(A,2))"){
            currentStatus="[2, 2, 2]";
            return true;
        }
        else
            return false;
    }

    function (Term1A!judge(A,2)|Ajudge(B,3), Term1Ajudge(A,2)|A!judge(B,3), Term1Ajudge(A,2)|Ajudge(B,3), Term2A!judge(A,2), Term3Ajudge(A,2))(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1]" && action=="(Term1A!judge(A,2)|Ajudge(B,3), Term1Ajudge(A,2)|A!judge(B,3), Term1Ajudge(A,2)|Ajudge(B,3), Term2Ajudge(A,2), Term3Ajudge(A,2))"){
            currentStatus="[2, 2, 2]";
            return true;
        }
        else
            return false;
    }

    function (Term1A!judge(A,2)|Ajudge(B,3), Term1Ajudge(A,2)|A!judge(B,3), Term1Ajudge(A,2)|Ajudge(B,3), Term2A!judge(A,2), Term3A!judge(A,2))(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1]" && action=="(Term1A!judge(A,2)|Ajudge(B,3), Term1Ajudge(A,2)|A!judge(B,3), Term1Ajudge(A,2)|Ajudge(B,3), Term2Ajudge(A,2), Term3Ajudge(A,2))"){
            currentStatus="[2, 2, 2]";
            return true;
        }
        else
            return false;
    }

    function (Term1A!judge(A,2)|A!judge(B,3), Term2Ajudge(A,2), Term3Ajudge(A,2))(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1]" && action=="(Term1A!judge(A,2)|Ajudge(B,3), Term1Ajudge(A,2)|A!judge(B,3), Term1Ajudge(A,2)|Ajudge(B,3), Term2Ajudge(A,2), Term3Ajudge(A,2))"){
            currentStatus="[2, 2, 2]";
            return true;
        }
        else
            return false;
    }

    function (Term1A!judge(A,2)|A!judge(B,3), Term2Ajudge(A,2), Term3A!judge(A,2))(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1]" && action=="(Term1A!judge(A,2)|Ajudge(B,3), Term1Ajudge(A,2)|A!judge(B,3), Term1Ajudge(A,2)|Ajudge(B,3), Term2Ajudge(A,2), Term3Ajudge(A,2))"){
            currentStatus="[2, 2, 2]";
            return true;
        }
        else
            return false;
    }

    function (Term1A!judge(A,2)|A!judge(B,3), Term2A!judge(A,2), Term3Ajudge(A,2))(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1]" && action=="(Term1A!judge(A,2)|Ajudge(B,3), Term1Ajudge(A,2)|A!judge(B,3), Term1Ajudge(A,2)|Ajudge(B,3), Term2Ajudge(A,2), Term3Ajudge(A,2))"){
            currentStatus="[2, 2, 2]";
            return true;
        }
        else
            return false;
    }

    function (Term1A!judge(A,2)|A!judge(B,3), Term2A!judge(A,2), Term3A!judge(A,2))(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1]" && action=="(Term1A!judge(A,2)|Ajudge(B,3), Term1Ajudge(A,2)|A!judge(B,3), Term1Ajudge(A,2)|Ajudge(B,3), Term2Ajudge(A,2), Term3Ajudge(A,2))"){
            currentStatus="[2, 2, 2]";
            return true;
        }
        else
            return false;
    }

    function (Term1Asaterer, Term2Asat, Term3Asat)(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1]" && action=="(Term1A!judge(A,2)|Ajudge(B,3), Term1Ajudge(A,2)|A!judge(B,3), Term1Ajudge(A,2)|Ajudge(B,3), Term2Ajudge(A,2), Term3Ajudge(A,2))"){
            currentStatus="[2, 2, 2]";
            return true;
        }
        else
            return false;
    }

    function (Term1Asaterer, Term2Asat, Term3Avio)(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1]" && action=="(Term1A!judge(A,2)|Ajudge(B,3), Term1Ajudge(A,2)|A!judge(B,3), Term1Ajudge(A,2)|Ajudge(B,3), Term2Ajudge(A,2), Term3Ajudge(A,2))"){
            currentStatus="[2, 2, 2]";
            return true;
        }
        else
            return false;
    }

    function (Term1Asaterer, Term2Avio, Term3Asat)(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1]" && action=="(Term1A!judge(A,2)|Ajudge(B,3), Term1Ajudge(A,2)|A!judge(B,3), Term1Ajudge(A,2)|Ajudge(B,3), Term2Ajudge(A,2), Term3Ajudge(A,2))"){
            currentStatus="[2, 2, 2]";
            return true;
        }
        else
            return false;
    }

    function (Term1Asaterer, Term2Avio, Term3Avio)(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1]" && action=="(Term1A!judge(A,2)|Ajudge(B,3), Term1Ajudge(A,2)|A!judge(B,3), Term1Ajudge(A,2)|Ajudge(B,3), Term2Ajudge(A,2), Term3Ajudge(A,2))"){
            currentStatus="[2, 2, 2]";
            return true;
        }
        else
            return false;
    }

    function (Term1Avioerer, Term2Asat, Term3Asat)(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1]" && action=="(Term1A!judge(A,2)|Ajudge(B,3), Term1Ajudge(A,2)|A!judge(B,3), Term1Ajudge(A,2)|Ajudge(B,3), Term2Ajudge(A,2), Term3Ajudge(A,2))"){
            currentStatus="[2, 2, 2]";
            return true;
        }
        else
            return false;
    }

    function (Term1Avioerer, Term2Asat, Term3Avio)(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1]" && action=="(Term1A!judge(A,2)|Ajudge(B,3), Term1Ajudge(A,2)|A!judge(B,3), Term1Ajudge(A,2)|Ajudge(B,3), Term2Ajudge(A,2), Term3Ajudge(A,2))"){
            currentStatus="[2, 2, 2]";
            return true;
        }
        else
            return false;
    }

    function (Term1Avioerer, Term2Avio, Term3Asat)(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1]" && action=="(Term1A!judge(A,2)|Ajudge(B,3), Term1Ajudge(A,2)|A!judge(B,3), Term1Ajudge(A,2)|Ajudge(B,3), Term2Ajudge(A,2), Term3Ajudge(A,2))"){
            currentStatus="[2, 2, 2]";
            return true;
        }
        else
            return false;
    }

    function (Term1Avioerer, Term2Avio, Term3Avio)(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1]" && action=="(Term1A!judge(A,2)|Ajudge(B,3), Term1Ajudge(A,2)|A!judge(B,3), Term1Ajudge(A,2)|Ajudge(B,3), Term2Ajudge(A,2), Term3Ajudge(A,2))"){
            currentStatus="[2, 2, 2]";
            return true;
        }
        else
            return false;
    }

    function (Term1Asaterer, Term2Asat)(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1]" && action=="(Term1A!judge(A,2)|Ajudge(B,3), Term1Ajudge(A,2)|A!judge(B,3), Term1Ajudge(A,2)|Ajudge(B,3), Term2Ajudge(A,2), Term3Ajudge(A,2))"){
            currentStatus="[2, 2, 2]";
            return true;
        }
        else
            return false;
    }

    function (Term1Asaterer, Term2Avio)(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1]" && action=="(Term1A!judge(A,2)|Ajudge(B,3), Term1Ajudge(A,2)|A!judge(B,3), Term1Ajudge(A,2)|Ajudge(B,3), Term2Ajudge(A,2), Term3Ajudge(A,2))"){
            currentStatus="[2, 2, 2]";
            return true;
        }
        else
            return false;
    }

    function (Term1Avioerer, Term2Asat)(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1]" && action=="(Term1A!judge(A,2)|Ajudge(B,3), Term1Ajudge(A,2)|A!judge(B,3), Term1Ajudge(A,2)|Ajudge(B,3), Term2Ajudge(A,2), Term3Ajudge(A,2))"){
            currentStatus="[2, 2, 2]";
            return true;
        }
        else
            return false;
    }

    function (Term1Avioerer, Term2Avio)(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1]" && action=="(Term1A!judge(A,2)|Ajudge(B,3), Term1Ajudge(A,2)|A!judge(B,3), Term1Ajudge(A,2)|Ajudge(B,3), Term2Ajudge(A,2), Term3Ajudge(A,2))"){
            currentStatus="[2, 2, 2]";
            return true;
        }
        else
            return false;
    }

    function (Term1Asaterer, Term3Asat)(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1]" && action=="(Term1A!judge(A,2)|Ajudge(B,3), Term1Ajudge(A,2)|A!judge(B,3), Term1Ajudge(A,2)|Ajudge(B,3), Term2Ajudge(A,2), Term3Ajudge(A,2))"){
            currentStatus="[2, 2, 2]";
            return true;
        }
        else
            return false;
    }

    function (Term1Asaterer, Term3Avio)(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1]" && action=="(Term1A!judge(A,2)|Ajudge(B,3), Term1Ajudge(A,2)|A!judge(B,3), Term1Ajudge(A,2)|Ajudge(B,3), Term2Ajudge(A,2), Term3Ajudge(A,2))"){
            currentStatus="[2, 2, 2]";
            return true;
        }
        else
            return false;
    }

    function (Term1Avioerer, Term3Asat)(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1]" && action=="(Term1A!judge(A,2)|Ajudge(B,3), Term1Ajudge(A,2)|A!judge(B,3), Term1Ajudge(A,2)|Ajudge(B,3), Term2Ajudge(A,2), Term3Ajudge(A,2))"){
            currentStatus="[2, 2, 2]";
            return true;
        }
        else
            return false;
    }

    function (Term1Avioerer, Term3Avio)(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1]" && action=="(Term1A!judge(A,2)|Ajudge(B,3), Term1Ajudge(A,2)|A!judge(B,3), Term1Ajudge(A,2)|Ajudge(B,3), Term2Ajudge(A,2), Term3Ajudge(A,2))"){
            currentStatus="[2, 2, 2]";
            return true;
        }
        else
            return false;
    }

    function (Term1Asaterer)(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1]" && action=="(Term1A!judge(A,2)|Ajudge(B,3), Term1Ajudge(A,2)|A!judge(B,3), Term1Ajudge(A,2)|Ajudge(B,3), Term2Ajudge(A,2), Term3Ajudge(A,2))"){
            currentStatus="[2, 2, 2]";
            return true;
        }
        else
            return false;
    }

    function (Term1Avioerer)(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1]" && action=="(Term1A!judge(A,2)|Ajudge(B,3), Term1Ajudge(A,2)|A!judge(B,3), Term1Ajudge(A,2)|Ajudge(B,3), Term2Ajudge(A,2), Term3Ajudge(A,2))"){
            currentStatus="[2, 2, 2]";
            return true;
        }
        else
            return false;
    }

    function (Term2Asat, Term3Asat)(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1]" && action=="(Term1A!judge(A,2)|Ajudge(B,3), Term1Ajudge(A,2)|A!judge(B,3), Term1Ajudge(A,2)|Ajudge(B,3), Term2Ajudge(A,2), Term3Ajudge(A,2))"){
            currentStatus="[2, 2, 2]";
            return true;
        }
        else
            return false;
    }

    function (Term2Asat, Term3Avio)(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1]" && action=="(Term1A!judge(A,2)|Ajudge(B,3), Term1Ajudge(A,2)|A!judge(B,3), Term1Ajudge(A,2)|Ajudge(B,3), Term2Ajudge(A,2), Term3Ajudge(A,2))"){
            currentStatus="[2, 2, 2]";
            return true;
        }
        else
            return false;
    }

    function (Term2Avio, Term3Asat)(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1]" && action=="(Term1A!judge(A,2)|Ajudge(B,3), Term1Ajudge(A,2)|A!judge(B,3), Term1Ajudge(A,2)|Ajudge(B,3), Term2Ajudge(A,2), Term3Ajudge(A,2))"){
            currentStatus="[2, 2, 2]";
            return true;
        }
        else
            return false;
    }

    function (Term2Avio, Term3Avio)(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1]" && action=="(Term1A!judge(A,2)|Ajudge(B,3), Term1Ajudge(A,2)|A!judge(B,3), Term1Ajudge(A,2)|Ajudge(B,3), Term2Ajudge(A,2), Term3Ajudge(A,2))"){
            currentStatus="[2, 2, 2]";
            return true;
        }
        else
            return false;
    }

    function (Term2Asat)(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1]" && action=="(Term1A!judge(A,2)|Ajudge(B,3), Term1Ajudge(A,2)|A!judge(B,3), Term1Ajudge(A,2)|Ajudge(B,3), Term2Ajudge(A,2), Term3Ajudge(A,2))"){
            currentStatus="[2, 2, 2]";
            return true;
        }
        else
            return false;
    }

    function (Term2Avio)(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1]" && action=="(Term1A!judge(A,2)|Ajudge(B,3), Term1Ajudge(A,2)|A!judge(B,3), Term1Ajudge(A,2)|Ajudge(B,3), Term2Ajudge(A,2), Term3Ajudge(A,2))"){
            currentStatus="[2, 2, 2]";
            return true;
        }
        else
            return false;
    }

    function (Term3Asat)(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1]" && action=="(Term1A!judge(A,2)|Ajudge(B,3), Term1Ajudge(A,2)|A!judge(B,3), Term1Ajudge(A,2)|Ajudge(B,3), Term2Ajudge(A,2), Term3Ajudge(A,2))"){
            currentStatus="[2, 2, 2]";
            return true;
        }
        else
            return false;
    }

    function (Term3Avio)(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1]" && action=="(Term1A!judge(A,2)|Ajudge(B,3), Term1Ajudge(A,2)|A!judge(B,3), Term1Ajudge(A,2)|Ajudge(B,3), Term2Ajudge(A,2), Term3Ajudge(A,2))"){
            currentStatus="[2, 2, 2]";
            return true;
        }
        else
            return false;
    }

}
//BCMETH means Blockchain Match Ethereum 
pragma solidity ^0.4.24;

contract BCMETH {
    String currentStatus;
    constructor () public {
        currentStatus="[1]";
    }

    function (Term1Ajudge(A,2))(String actionStr) public returns(bool){
        if(currentStatus=="[1]" && action=="(Term1Ajudge(A,2))"){
            currentStatus="[2]";
            return true;
        }
        else
            return false;
    }

    function (Term1A!judge(A,2))(String actionStr) public returns(bool){
        if(currentStatus=="[1]" && action=="(Term1Ajudge(A,2))"){
            currentStatus="[2]";
            return true;
        }
        else
            return false;
    }

    function (Term1Asatvcgfg)(String actionStr) public returns(bool){
        if(currentStatus=="[1]" && action=="(Term1Ajudge(A,2))"){
            currentStatus="[2]";
            return true;
        }
        else
            return false;
    }

    function (Term1Aviovcgfg)(String actionStr) public returns(bool){
        if(currentStatus=="[1]" && action=="(Term1Ajudge(A,2))"){
            currentStatus="[2]";
            return true;
        }
        else
            return false;
    }

}
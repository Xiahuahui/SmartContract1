//BCMETH means Blockchain Match Ethereum 
pragma solidity ^0.4.24;

contract BCMETH {
    String currentStatus;
    constructor () public {
        currentStatus="[1]";
    }

    function Ture-324(String actionStr) public returns(bool){
        if(currentStatus=="[1]" && action=="Ture-324"){
            currentStatus="[2]";
            return true;
        }
        else
            return false;
    }

    function Timeout-324(String actionStr) public returns(bool){
        if(currentStatus=="[1]" && action=="Ture-324"){
            currentStatus="[2]";
            return true;
        }
        else
            return false;
    }

    function Ture-3242(String actionStr) public returns(bool){
        if(currentStatus=="[1]" && action=="Ture-324"){
            currentStatus="[2]";
            return true;
        }
        else
            return false;
    }

    function Timeout-3242(String actionStr) public returns(bool){
        if(currentStatus=="[1]" && action=="Ture-324"){
            currentStatus="[2]";
            return true;
        }
        else
            return false;
    }

}
//BCMETH means Blockchain Match Ethereum 
pragma solidity ^0.4.24;

contract BCMETH {
    String currentStatus;
    constructor () public {
        currentStatus="[1, 1]";
    }

    function Ture-ytrty(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1]" && action=="Ture-ytrty"){
            currentStatus="[2, 1]";
            return true;
        }
        else
            return false;
    }

    function Timeout-ytrty(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1]" && action=="Ture-ytrty"){
            currentStatus="[2, 1]";
            return true;
        }
        else
            return false;
    }

    function Ture-gffdg(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1]" && action=="Ture-ytrty"){
            currentStatus="[2, 1]";
            return true;
        }
        else
            return false;
    }

    function Timeout-gffdg(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1]" && action=="Ture-ytrty"){
            currentStatus="[2, 1]";
            return true;
        }
        else
            return false;
    }

    function Ture-缴纳定金(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1]" && action=="Ture-ytrty"){
            currentStatus="[2, 1]";
            return true;
        }
        else
            return false;
    }

    function Timeout缴纳定金(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1]" && action=="Ture-ytrty"){
            currentStatus="[2, 1]";
            return true;
        }
        else
            return false;
    }

    function Ture-gffdg(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1]" && action=="Ture-ytrty"){
            currentStatus="[2, 1]";
            return true;
        }
        else
            return false;
    }

    function Timeout-gffdg(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1]" && action=="Ture-ytrty"){
            currentStatus="[2, 1]";
            return true;
        }
        else
            return false;
    }

    function Ture-gffdg(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1]" && action=="Ture-ytrty"){
            currentStatus="[2, 1]";
            return true;
        }
        else
            return false;
    }

    function Timeout-gffdg(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1]" && action=="Ture-ytrty"){
            currentStatus="[2, 1]";
            return true;
        }
        else
            return false;
    }

    function Ture-ytrty(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1]" && action=="Ture-ytrty"){
            currentStatus="[2, 1]";
            return true;
        }
        else
            return false;
    }

    function Timeout-ytrty(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1]" && action=="Ture-ytrty"){
            currentStatus="[2, 1]";
            return true;
        }
        else
            return false;
    }

    function Ture-租房(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1]" && action=="Ture-ytrty"){
            currentStatus="[2, 1]";
            return true;
        }
        else
            return false;
    }

    function Timeout租房(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1]" && action=="Ture-ytrty"){
            currentStatus="[2, 1]";
            return true;
        }
        else
            return false;
    }

    function Ture-ytrty(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1]" && action=="Ture-ytrty"){
            currentStatus="[2, 1]";
            return true;
        }
        else
            return false;
    }

    function Timeout-ytrty(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1]" && action=="Ture-ytrty"){
            currentStatus="[2, 1]";
            return true;
        }
        else
            return false;
    }

    function Ture-gffdg(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1]" && action=="Ture-ytrty"){
            currentStatus="[2, 1]";
            return true;
        }
        else
            return false;
    }

    function Timeout-gffdg(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1]" && action=="Ture-ytrty"){
            currentStatus="[2, 1]";
            return true;
        }
        else
            return false;
    }

    function Ture-gffdg(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1]" && action=="Ture-ytrty"){
            currentStatus="[2, 1]";
            return true;
        }
        else
            return false;
    }

    function Timeout-gffdg(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1]" && action=="Ture-ytrty"){
            currentStatus="[2, 1]";
            return true;
        }
        else
            return false;
    }

    function Ture-缴纳定金(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1]" && action=="Ture-ytrty"){
            currentStatus="[2, 1]";
            return true;
        }
        else
            return false;
    }

    function Timeout缴纳定金(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1]" && action=="Ture-ytrty"){
            currentStatus="[2, 1]";
            return true;
        }
        else
            return false;
    }

    function Ture-租房(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1]" && action=="Ture-ytrty"){
            currentStatus="[2, 1]";
            return true;
        }
        else
            return false;
    }

    function Timeout租房(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1]" && action=="Ture-ytrty"){
            currentStatus="[2, 1]";
            return true;
        }
        else
            return false;
    }

    function Ture-缴纳定金(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1]" && action=="Ture-ytrty"){
            currentStatus="[2, 1]";
            return true;
        }
        else
            return false;
    }

    function Timeout缴纳定金(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1]" && action=="Ture-ytrty"){
            currentStatus="[2, 1]";
            return true;
        }
        else
            return false;
    }

    function Ture-租房(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1]" && action=="Ture-ytrty"){
            currentStatus="[2, 1]";
            return true;
        }
        else
            return false;
    }

    function Timeout租房(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1]" && action=="Ture-ytrty"){
            currentStatus="[2, 1]";
            return true;
        }
        else
            return false;
    }

    function Ture-ytrty(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1]" && action=="Ture-ytrty"){
            currentStatus="[2, 1]";
            return true;
        }
        else
            return false;
    }

    function Timeout-ytrty(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1]" && action=="Ture-ytrty"){
            currentStatus="[2, 1]";
            return true;
        }
        else
            return false;
    }

    function Ture-ytrty(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1]" && action=="Ture-ytrty"){
            currentStatus="[2, 1]";
            return true;
        }
        else
            return false;
    }

    function Timeout-ytrty(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1]" && action=="Ture-ytrty"){
            currentStatus="[2, 1]";
            return true;
        }
        else
            return false;
    }

    function Ture-租房(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1]" && action=="Ture-ytrty"){
            currentStatus="[2, 1]";
            return true;
        }
        else
            return false;
    }

    function Timeout租房(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1]" && action=="Ture-ytrty"){
            currentStatus="[2, 1]";
            return true;
        }
        else
            return false;
    }

    function Ture-租房(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1]" && action=="Ture-ytrty"){
            currentStatus="[2, 1]";
            return true;
        }
        else
            return false;
    }

    function Timeout租房(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1]" && action=="Ture-ytrty"){
            currentStatus="[2, 1]";
            return true;
        }
        else
            return false;
    }

    function Ture-缴纳定金(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1]" && action=="Ture-ytrty"){
            currentStatus="[2, 1]";
            return true;
        }
        else
            return false;
    }

    function Timeout缴纳定金(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1]" && action=="Ture-ytrty"){
            currentStatus="[2, 1]";
            return true;
        }
        else
            return false;
    }

    function Ture-缴纳定金(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1]" && action=="Ture-ytrty"){
            currentStatus="[2, 1]";
            return true;
        }
        else
            return false;
    }

    function Timeout缴纳定金(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1]" && action=="Ture-ytrty"){
            currentStatus="[2, 1]";
            return true;
        }
        else
            return false;
    }

}
//BCMETH means Blockchain Match Ethereum 
pragma solidity ^0.4.24;

contract BCMETH {
    String currentStatus;
    constructor () public {
        currentStatus="[2, 1, 1, 1, 1]";
    }

    function [A]预存书款100元(String actionStr) public returns(bool){
        if(currentStatus=="[2, 1, 1, 1, 1]" && action=="[A]预存书款100元"){
            currentStatus="[3, 1, 1, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function [A]预存书款100元超时(String actionStr) public returns(bool){
        if(currentStatus=="[2, 1, 1, 1, 1]" && action=="[A]预存书款100元"){
            currentStatus="[3, 1, 1, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function (String actionStr) public returns(bool){
        if(currentStatus=="[2, 1, 1, 1, 1]" && action=="[A]预存书款100元"){
            currentStatus="[3, 1, 1, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function (String actionStr) public returns(bool){
        if(currentStatus=="[2, 1, 1, 1, 1]" && action=="[A]预存书款100元"){
            currentStatus="[3, 1, 1, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function [B]给李华发送书籍(String actionStr) public returns(bool){
        if(currentStatus=="[2, 1, 1, 1, 1]" && action=="[A]预存书款100元"){
            currentStatus="[3, 1, 1, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function [B]给李华发送书籍超时(String actionStr) public returns(bool){
        if(currentStatus=="[2, 1, 1, 1, 1]" && action=="[A]预存书款100元"){
            currentStatus="[3, 1, 1, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function (String actionStr) public returns(bool){
        if(currentStatus=="[2, 1, 1, 1, 1]" && action=="[A]预存书款100元"){
            currentStatus="[3, 1, 1, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function (String actionStr) public returns(bool){
        if(currentStatus=="[2, 1, 1, 1, 1]" && action=="[A]预存书款100元"){
            currentStatus="[3, 1, 1, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function (String actionStr) public returns(bool){
        if(currentStatus=="[2, 1, 1, 1, 1]" && action=="[A]预存书款100元"){
            currentStatus="[3, 1, 1, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function (String actionStr) public returns(bool){
        if(currentStatus=="[2, 1, 1, 1, 1]" && action=="[A]预存书款100元"){
            currentStatus="[3, 1, 1, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function [A]确认收到书籍(String actionStr) public returns(bool){
        if(currentStatus=="[2, 1, 1, 1, 1]" && action=="[A]预存书款100元"){
            currentStatus="[3, 1, 1, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function [A]确认收到书籍超时(String actionStr) public returns(bool){
        if(currentStatus=="[2, 1, 1, 1, 1]" && action=="[A]预存书款100元"){
            currentStatus="[3, 1, 1, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function [C]将书款返回给李华(String actionStr) public returns(bool){
        if(currentStatus=="[2, 1, 1, 1, 1]" && action=="[A]预存书款100元"){
            currentStatus="[3, 1, 1, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function (String actionStr) public returns(bool){
        if(currentStatus=="[2, 1, 1, 1, 1]" && action=="[A]预存书款100元"){
            currentStatus="[3, 1, 1, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function [B]!judge([B],C2.vio)(String actionStr) public returns(bool){
        if(currentStatus=="[2, 1, 1, 1, 1]" && action=="[A]预存书款100元"){
            currentStatus="[3, 1, 1, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function [B]judge([B],C2.vio)(String actionStr) public returns(bool){
        if(currentStatus=="[2, 1, 1, 1, 1]" && action=="[A]预存书款100元"){
            currentStatus="[3, 1, 1, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function [C]将书款转给书店(String actionStr) public returns(bool){
        if(currentStatus=="[2, 1, 1, 1, 1]" && action=="[A]预存书款100元"){
            currentStatus="[3, 1, 1, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function [C]将书款转给书店(String actionStr) public returns(bool){
        if(currentStatus=="[2, 1, 1, 1, 1]" && action=="[A]预存书款100元"){
            currentStatus="[3, 1, 1, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function (String actionStr) public returns(bool){
        if(currentStatus=="[2, 1, 1, 1, 1]" && action=="[A]预存书款100元"){
            currentStatus="[3, 1, 1, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function [C]将书款返回给李华(String actionStr) public returns(bool){
        if(currentStatus=="[2, 1, 1, 1, 1]" && action=="[A]预存书款100元"){
            currentStatus="[3, 1, 1, 1, 1]";
            return true;
        }
        else
            return false;
    }

}
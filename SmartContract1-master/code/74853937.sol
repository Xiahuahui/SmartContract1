//BCMETH means Blockchain Match Ethereum 
pragma solidity ^0.4.24;

contract BCMETH {
    String currentStatus;
    constructor () public {
        currentStatus="[1, 1, 1, 1]";
    }

    function (  Term0: execute judge())(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1, 1]" && action=="(  Term0: execute judge())"){
            currentStatus="[2, 1, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function (  Term0: timeout judge())(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1, 1]" && action=="(  Term0: execute judge())"){
            currentStatus="[2, 1, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function (Term0: execute 付款 )(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1, 1]" && action=="(  Term0: execute judge())"){
            currentStatus="[2, 1, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function (Term0: Violate 付款 )(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1, 1]" && action=="(  Term0: execute judge())"){
            currentStatus="[2, 1, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function ()(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1, 1]" && action=="(  Term0: execute judge())"){
            currentStatus="[2, 1, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function ()(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1, 1]" && action=="(  Term0: execute judge())"){
            currentStatus="[2, 1, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function ()(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1, 1]" && action=="(  Term0: execute judge())"){
            currentStatus="[2, 1, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function (  Term2: execute judge())(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1, 1]" && action=="(  Term0: execute judge())"){
            currentStatus="[2, 1, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function (  Term2: timeout judge())(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1, 1]" && action=="(  Term0: execute judge())"){
            currentStatus="[2, 1, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function (Term1: execute 发货 )(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1, 1]" && action=="(  Term0: execute judge())"){
            currentStatus="[2, 1, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function (Term1: Violate 发货 )(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1, 1]" && action=="(  Term0: execute judge())"){
            currentStatus="[2, 1, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function (  Term2: execute judge())(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1, 1]" && action=="(  Term0: execute judge())"){
            currentStatus="[2, 1, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function (  Term2: timeout judge())(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1, 1]" && action=="(  Term0: execute judge())"){
            currentStatus="[2, 1, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function (Term2: execute 确认 )(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1, 1]" && action=="(  Term0: execute judge())"){
            currentStatus="[2, 1, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function (Term2: Violate 确认 )(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1, 1]" && action=="(  Term0: execute judge())"){
            currentStatus="[2, 1, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function ()(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1, 1]" && action=="(  Term0: execute judge())"){
            currentStatus="[2, 1, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function (  Term2: execute judge())(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1, 1]" && action=="(  Term0: execute judge())"){
            currentStatus="[2, 1, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function (  Term2: timeout judge())(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1, 1]" && action=="(  Term0: execute judge())"){
            currentStatus="[2, 1, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function (Term2: execute 确认 )(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1, 1]" && action=="(  Term0: execute judge())"){
            currentStatus="[2, 1, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function (Term2: Violate 确认 )(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1, 1]" && action=="(  Term0: execute judge())"){
            currentStatus="[2, 1, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function (Term2: execute 确认 , Term3: execute 5656 )(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1, 1]" && action=="(  Term0: execute judge())"){
            currentStatus="[2, 1, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function (Term2: Violate 确认 , Term3: execute 5656 )(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1, 1]" && action=="(  Term0: execute judge())"){
            currentStatus="[2, 1, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function (Term2: execute 确认 )(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1, 1]" && action=="(  Term0: execute judge())"){
            currentStatus="[2, 1, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function (Term2: Violate 确认 )(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1, 1]" && action=="(  Term0: execute judge())"){
            currentStatus="[2, 1, 1, 1]";
            return true;
        }
        else
            return false;
    }

}
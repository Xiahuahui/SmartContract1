//BCMETH means Blockchain Match Ethereum 
pragma solidity ^0.4.24;

contract BCMETH {
    String currentStatus;
    constructor () public {
        currentStatus="[1, 1]";
    }

    function [judge(B,4]judge(B,4)(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1]" && action=="[judge(B,4]judge(B,4)"){
            currentStatus="[2, 1]";
            return true;
        }
        else
            return false;
    }

    function [!judge(B,4]!judge(B,4)(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1]" && action=="[judge(B,4]judge(B,4)"){
            currentStatus="[2, 1]";
            return true;
        }
        else
            return false;
    }

    function [A]erer(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1]" && action=="[judge(B,4]judge(B,4)"){
            currentStatus="[2, 1]";
            return true;
        }
        else
            return false;
    }

    function [A]erer超时(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1]" && action=="[judge(B,4]judge(B,4)"){
            currentStatus="[2, 1]";
            return true;
        }
        else
            return false;
    }

    function (String actionStr) public returns(bool){
        if(currentStatus=="[1, 1]" && action=="[judge(B,4]judge(B,4)"){
            currentStatus="[2, 1]";
            return true;
        }
        else
            return false;
    }

    function (String actionStr) public returns(bool){
        if(currentStatus=="[1, 1]" && action=="[judge(B,4]judge(B,4)"){
            currentStatus="[2, 1]";
            return true;
        }
        else
            return false;
    }

    function (String actionStr) public returns(bool){
        if(currentStatus=="[1, 1]" && action=="[judge(B,4]judge(B,4)"){
            currentStatus="[2, 1]";
            return true;
        }
        else
            return false;
    }

    function [B]tergfg(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1]" && action=="[judge(B,4]judge(B,4)"){
            currentStatus="[2, 1]";
            return true;
        }
        else
            return false;
    }

    function [B]tergfg超时(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1]" && action=="[judge(B,4]judge(B,4)"){
            currentStatus="[2, 1]";
            return true;
        }
        else
            return false;
    }

}
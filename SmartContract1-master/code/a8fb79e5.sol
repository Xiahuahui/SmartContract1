//BCMETH means Blockchain Match Ethereum 
pragma solidity ^0.4.24;

contract BCMETH {
    String currentStatus;
    constructor () public {
        currentStatus="[1]";
    }

    function Ture-rrr(String actionStr) public returns(bool){
        if(currentStatus=="[1]" && action=="Ture-rrr"){
            currentStatus="[2]";
            return true;
        }
        else
            return false;
    }

    function Timeout-rrr(String actionStr) public returns(bool){
        if(currentStatus=="[1]" && action=="Ture-rrr"){
            currentStatus="[2]";
            return true;
        }
        else
            return false;
    }

    function Ture-二二(String actionStr) public returns(bool){
        if(currentStatus=="[1]" && action=="Ture-rrr"){
            currentStatus="[2]";
            return true;
        }
        else
            return false;
    }

    function Timeout-二二(String actionStr) public returns(bool){
        if(currentStatus=="[1]" && action=="Ture-rrr"){
            currentStatus="[2]";
            return true;
        }
        else
            return false;
    }

}
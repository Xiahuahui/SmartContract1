//BCMETH means Blockchain Match Ethereum 
pragma solidity ^0.4.24;

contract BCMETH {
    String currentStatus;
    constructor () public {
        currentStatus="[1, 1]";
    }

    function (  Term2Bjudge(A))(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1]" && action=="(  Term2Bjudge(A))"){
            currentStatus="[1, 2]";
            return true;
        }
        else
            return false;
    }

    function (  Term2B!judge(A))(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1]" && action=="(  Term2Bjudge(A))"){
            currentStatus="[1, 2]";
            return true;
        }
        else
            return false;
    }

    function (Term2B333)(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1]" && action=="(  Term2Bjudge(A))"){
            currentStatus="[1, 2]";
            return true;
        }
        else
            return false;
    }

    function (Term2Btimeout333)(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1]" && action=="(  Term2Bjudge(A))"){
            currentStatus="[1, 2]";
            return true;
        }
        else
            return false;
    }

    function (  Term1A )(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1]" && action=="(  Term2Bjudge(A))"){
            currentStatus="[1, 2]";
            return true;
        }
        else
            return false;
    }

    function (  Term1A )(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1]" && action=="(  Term2Bjudge(A))"){
            currentStatus="[1, 2]";
            return true;
        }
        else
            return false;
    }

    function (  Term1A )(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1]" && action=="(  Term2Bjudge(A))"){
            currentStatus="[1, 2]";
            return true;
        }
        else
            return false;
    }

    function (Term1Artrt)(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1]" && action=="(  Term2Bjudge(A))"){
            currentStatus="[1, 2]";
            return true;
        }
        else
            return false;
    }

    function (Term1Atimeoutrtrt)(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1]" && action=="(  Term2Bjudge(A))"){
            currentStatus="[1, 2]";
            return true;
        }
        else
            return false;
    }

}
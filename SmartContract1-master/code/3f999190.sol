//BCMETH means Blockchain Match Ethereum 
pragma solidity ^0.4.24;

contract BCMETH {
    String currentStatus;
    constructor () public {
        currentStatus="[1]";
    }

    function Ture-vxc(String actionStr) public returns(bool){
        if(currentStatus=="[1]" && action=="Ture-vxc"){
            currentStatus="[2]";
            return true;
        }
        else
            return false;
    }

    function Timeout-vxc(String actionStr) public returns(bool){
        if(currentStatus=="[1]" && action=="Ture-vxc"){
            currentStatus="[2]";
            return true;
        }
        else
            return false;
    }

    function Ture-程序V型(String actionStr) public returns(bool){
        if(currentStatus=="[1]" && action=="Ture-vxc"){
            currentStatus="[2]";
            return true;
        }
        else
            return false;
    }

    function Timeout-程序V型(String actionStr) public returns(bool){
        if(currentStatus=="[1]" && action=="Ture-vxc"){
            currentStatus="[2]";
            return true;
        }
        else
            return false;
    }

}
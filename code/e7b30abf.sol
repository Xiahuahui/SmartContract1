//BCMETH means Blockchain Match Ethereum 
pragma solidity ^0.4.24;

contract BCMETH {
    String currentStatus;
    constructor () public {
        currentStatus="[1, 1, 1]";
    }

    function Ture-werq(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1]" && action=="Ture-werq"){
            currentStatus="[2, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function Timeout-werq(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1]" && action=="Ture-werq"){
            currentStatus="[2, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function Timeout-03(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1]" && action=="Ture-werq"){
            currentStatus="[2, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function Timeout-14(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1]" && action=="Ture-werq"){
            currentStatus="[2, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function Ture-ereqw(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1]" && action=="Ture-werq"){
            currentStatus="[2, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function Timeout-ereqw(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1]" && action=="Ture-werq"){
            currentStatus="[2, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function Timeout-03(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1]" && action=="Ture-werq"){
            currentStatus="[2, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function Timeout-14(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1]" && action=="Ture-werq"){
            currentStatus="[2, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function Timeout-03(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1]" && action=="Ture-werq"){
            currentStatus="[2, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function Timeout-14(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1]" && action=="Ture-werq"){
            currentStatus="[2, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function Ture-werq(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1]" && action=="Ture-werq"){
            currentStatus="[2, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function Timeout-werq(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1]" && action=="Ture-werq"){
            currentStatus="[2, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function Ture-14(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1]" && action=="Ture-werq"){
            currentStatus="[2, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function Ture-werq(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1]" && action=="Ture-werq"){
            currentStatus="[2, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function Timeout-werq(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1]" && action=="Ture-werq"){
            currentStatus="[2, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function Timeout-03(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1]" && action=="Ture-werq"){
            currentStatus="[2, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function Ture-03(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1]" && action=="Ture-werq"){
            currentStatus="[2, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function Timeout-14(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1]" && action=="Ture-werq"){
            currentStatus="[2, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function Timeout-03(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1]" && action=="Ture-werq"){
            currentStatus="[2, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function Timeout-14(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1]" && action=="Ture-werq"){
            currentStatus="[2, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function Ture-ereqw(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1]" && action=="Ture-werq"){
            currentStatus="[2, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function Timeout-ereqw(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1]" && action=="Ture-werq"){
            currentStatus="[2, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function Ture-14(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1]" && action=="Ture-werq"){
            currentStatus="[2, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function Ture-ereqw(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1]" && action=="Ture-werq"){
            currentStatus="[2, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function Timeout-ereqw(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1]" && action=="Ture-werq"){
            currentStatus="[2, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function Timeout-03(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1]" && action=="Ture-werq"){
            currentStatus="[2, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function Ture-14(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1]" && action=="Ture-werq"){
            currentStatus="[2, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function Timeout-03(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1]" && action=="Ture-werq"){
            currentStatus="[2, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function Ture-werq(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1]" && action=="Ture-werq"){
            currentStatus="[2, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function Timeout-werq(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1]" && action=="Ture-werq"){
            currentStatus="[2, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function Ture-wreq(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1]" && action=="Ture-werq"){
            currentStatus="[2, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function Timeout-wreq(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1]" && action=="Ture-werq"){
            currentStatus="[2, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function Ture-werq(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1]" && action=="Ture-werq"){
            currentStatus="[2, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function Timeout-werq(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1]" && action=="Ture-werq"){
            currentStatus="[2, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function Ture-qwerqr(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1]" && action=="Ture-werq"){
            currentStatus="[2, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function Timeout-qwerqr(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1]" && action=="Ture-werq"){
            currentStatus="[2, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function Timeout-14(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1]" && action=="Ture-werq"){
            currentStatus="[2, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function Ture-03(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1]" && action=="Ture-werq"){
            currentStatus="[2, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function Ture-14(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1]" && action=="Ture-werq"){
            currentStatus="[2, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function Timeout-03(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1]" && action=="Ture-werq"){
            currentStatus="[2, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function Ture-14(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1]" && action=="Ture-werq"){
            currentStatus="[2, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function Ture-ereqw(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1]" && action=="Ture-werq"){
            currentStatus="[2, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function Timeout-ereqw(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1]" && action=="Ture-werq"){
            currentStatus="[2, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function Ture-wreq(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1]" && action=="Ture-werq"){
            currentStatus="[2, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function Timeout-wreq(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1]" && action=="Ture-werq"){
            currentStatus="[2, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function Ture-ereqw(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1]" && action=="Ture-werq"){
            currentStatus="[2, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function Timeout-ereqw(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1]" && action=="Ture-werq"){
            currentStatus="[2, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function Ture-wreq(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1]" && action=="Ture-werq"){
            currentStatus="[2, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function Timeout-wreq(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1]" && action=="Ture-werq"){
            currentStatus="[2, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function Ture-werq(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1]" && action=="Ture-werq"){
            currentStatus="[2, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function Timeout-werq(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1]" && action=="Ture-werq"){
            currentStatus="[2, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function Ture-werq(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1]" && action=="Ture-werq"){
            currentStatus="[2, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function Timeout-werq(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1]" && action=="Ture-werq"){
            currentStatus="[2, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function Timeout-14(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1]" && action=="Ture-werq"){
            currentStatus="[2, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function Timeout-14(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1]" && action=="Ture-werq"){
            currentStatus="[2, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function Ture-qwerqr(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1]" && action=="Ture-werq"){
            currentStatus="[2, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function Timeout-qwerqr(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1]" && action=="Ture-werq"){
            currentStatus="[2, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function Ture-wreq(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1]" && action=="Ture-werq"){
            currentStatus="[2, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function Timeout-wreq(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1]" && action=="Ture-werq"){
            currentStatus="[2, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function Ture-wreq(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1]" && action=="Ture-werq"){
            currentStatus="[2, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function Timeout-wreq(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1]" && action=="Ture-werq"){
            currentStatus="[2, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function Ture-ereqw(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1]" && action=="Ture-werq"){
            currentStatus="[2, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function Timeout-ereqw(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1]" && action=="Ture-werq"){
            currentStatus="[2, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function Ture-ereqw(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1]" && action=="Ture-werq"){
            currentStatus="[2, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function Timeout-ereqw(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1]" && action=="Ture-werq"){
            currentStatus="[2, 1, 1]";
            return true;
        }
        else
            return false;
    }

}
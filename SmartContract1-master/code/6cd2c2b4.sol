//BCMETH means Blockchain Match Ethereum 
pragma solidity ^0.4.24;

contract BCMETH {
    String currentStatus;
    constructor () public {
        currentStatus="[1, 1]";
    }

    function Ture-gdsfg(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1]" && action=="Ture-gdsfg"){
            currentStatus="[2, 1]";
            return true;
        }
        else
            return false;
    }

    function Timeout-gdsfg(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1]" && action=="Ture-gdsfg"){
            currentStatus="[2, 1]";
            return true;
        }
        else
            return false;
    }

    function Ture-gdfgs(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1]" && action=="Ture-gdsfg"){
            currentStatus="[2, 1]";
            return true;
        }
        else
            return false;
    }

    function Timeout-gdfgs(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1]" && action=="Ture-gdsfg"){
            currentStatus="[2, 1]";
            return true;
        }
        else
            return false;
    }

    function Ture-奋斗故事大概(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1]" && action=="Ture-gdsfg"){
            currentStatus="[2, 1]";
            return true;
        }
        else
            return false;
    }

    function Timeout-奋斗故事大概(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1]" && action=="Ture-gdsfg"){
            currentStatus="[2, 1]";
            return true;
        }
        else
            return false;
    }

    function Ture-gdfgs(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1]" && action=="Ture-gdsfg"){
            currentStatus="[2, 1]";
            return true;
        }
        else
            return false;
    }

    function Timeout-gdfgs(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1]" && action=="Ture-gdsfg"){
            currentStatus="[2, 1]";
            return true;
        }
        else
            return false;
    }

    function Ture-gdfgs(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1]" && action=="Ture-gdsfg"){
            currentStatus="[2, 1]";
            return true;
        }
        else
            return false;
    }

    function Timeout-gdfgs(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1]" && action=="Ture-gdsfg"){
            currentStatus="[2, 1]";
            return true;
        }
        else
            return false;
    }

    function Ture-gdsfg(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1]" && action=="Ture-gdsfg"){
            currentStatus="[2, 1]";
            return true;
        }
        else
            return false;
    }

    function Timeout-gdsfg(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1]" && action=="Ture-gdsfg"){
            currentStatus="[2, 1]";
            return true;
        }
        else
            return false;
    }

    function Ture-对方告诉大哥(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1]" && action=="Ture-gdsfg"){
            currentStatus="[2, 1]";
            return true;
        }
        else
            return false;
    }

    function Timeout-对方告诉大哥(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1]" && action=="Ture-gdsfg"){
            currentStatus="[2, 1]";
            return true;
        }
        else
            return false;
    }

    function Ture-gdsfg(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1]" && action=="Ture-gdsfg"){
            currentStatus="[2, 1]";
            return true;
        }
        else
            return false;
    }

    function Timeout-gdsfg(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1]" && action=="Ture-gdsfg"){
            currentStatus="[2, 1]";
            return true;
        }
        else
            return false;
    }

    function Ture-gdfgs(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1]" && action=="Ture-gdsfg"){
            currentStatus="[2, 1]";
            return true;
        }
        else
            return false;
    }

    function Timeout-gdfgs(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1]" && action=="Ture-gdsfg"){
            currentStatus="[2, 1]";
            return true;
        }
        else
            return false;
    }

    function Ture-gdfgs(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1]" && action=="Ture-gdsfg"){
            currentStatus="[2, 1]";
            return true;
        }
        else
            return false;
    }

    function Timeout-gdfgs(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1]" && action=="Ture-gdsfg"){
            currentStatus="[2, 1]";
            return true;
        }
        else
            return false;
    }

    function Ture-奋斗故事大概(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1]" && action=="Ture-gdsfg"){
            currentStatus="[2, 1]";
            return true;
        }
        else
            return false;
    }

    function Timeout-奋斗故事大概(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1]" && action=="Ture-gdsfg"){
            currentStatus="[2, 1]";
            return true;
        }
        else
            return false;
    }

    function Ture-对方告诉大哥(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1]" && action=="Ture-gdsfg"){
            currentStatus="[2, 1]";
            return true;
        }
        else
            return false;
    }

    function Timeout-对方告诉大哥(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1]" && action=="Ture-gdsfg"){
            currentStatus="[2, 1]";
            return true;
        }
        else
            return false;
    }

    function Ture-奋斗故事大概(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1]" && action=="Ture-gdsfg"){
            currentStatus="[2, 1]";
            return true;
        }
        else
            return false;
    }

    function Timeout-奋斗故事大概(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1]" && action=="Ture-gdsfg"){
            currentStatus="[2, 1]";
            return true;
        }
        else
            return false;
    }

    function Ture-对方告诉大哥(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1]" && action=="Ture-gdsfg"){
            currentStatus="[2, 1]";
            return true;
        }
        else
            return false;
    }

    function Timeout-对方告诉大哥(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1]" && action=="Ture-gdsfg"){
            currentStatus="[2, 1]";
            return true;
        }
        else
            return false;
    }

    function Ture-gdsfg(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1]" && action=="Ture-gdsfg"){
            currentStatus="[2, 1]";
            return true;
        }
        else
            return false;
    }

    function Timeout-gdsfg(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1]" && action=="Ture-gdsfg"){
            currentStatus="[2, 1]";
            return true;
        }
        else
            return false;
    }

    function Ture-gdsfg(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1]" && action=="Ture-gdsfg"){
            currentStatus="[2, 1]";
            return true;
        }
        else
            return false;
    }

    function Timeout-gdsfg(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1]" && action=="Ture-gdsfg"){
            currentStatus="[2, 1]";
            return true;
        }
        else
            return false;
    }

    function Ture-对方告诉大哥(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1]" && action=="Ture-gdsfg"){
            currentStatus="[2, 1]";
            return true;
        }
        else
            return false;
    }

    function Timeout-对方告诉大哥(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1]" && action=="Ture-gdsfg"){
            currentStatus="[2, 1]";
            return true;
        }
        else
            return false;
    }

    function Ture-对方告诉大哥(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1]" && action=="Ture-gdsfg"){
            currentStatus="[2, 1]";
            return true;
        }
        else
            return false;
    }

    function Timeout-对方告诉大哥(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1]" && action=="Ture-gdsfg"){
            currentStatus="[2, 1]";
            return true;
        }
        else
            return false;
    }

    function Ture-奋斗故事大概(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1]" && action=="Ture-gdsfg"){
            currentStatus="[2, 1]";
            return true;
        }
        else
            return false;
    }

    function Timeout-奋斗故事大概(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1]" && action=="Ture-gdsfg"){
            currentStatus="[2, 1]";
            return true;
        }
        else
            return false;
    }

    function Ture-奋斗故事大概(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1]" && action=="Ture-gdsfg"){
            currentStatus="[2, 1]";
            return true;
        }
        else
            return false;
    }

    function Timeout-奋斗故事大概(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1]" && action=="Ture-gdsfg"){
            currentStatus="[2, 1]";
            return true;
        }
        else
            return false;
    }

}
//BCMETH means Blockchain Match Ethereum 
pragma solidity ^0.4.24;

contract BCMETH {
    String currentStatus;
    constructor () public {
        currentStatus="[1, 1]";
    }

    function Ture-vvvv(String actionStr) public returns(bool){
        if(currentStatus=="[2, 1]" && action=="Ture-vvvv"){
            currentStatus="[3, 1]";
            return true;
        }
        else
            return false;
    }

    function Timeout-vvvv(String actionStr) public returns(bool){
        if(currentStatus=="[2, 1]" && action=="Ture-vvvv"){
            currentStatus="[3, 1]";
            return true;
        }
        else
            return false;
    }

    function Timeout-03(String actionStr) public returns(bool){
        if(currentStatus=="[2, 1]" && action=="Ture-vvvv"){
            currentStatus="[3, 1]";
            return true;
        }
        else
            return false;
    }

    function Ture-03(String actionStr) public returns(bool){
        if(currentStatus=="[2, 1]" && action=="Ture-vvvv"){
            currentStatus="[3, 1]";
            return true;
        }
        else
            return false;
    }

    function Timeout-03(String actionStr) public returns(bool){
        if(currentStatus=="[2, 1]" && action=="Ture-vvvv"){
            currentStatus="[3, 1]";
            return true;
        }
        else
            return false;
    }

    function Ture-vvvv(String actionStr) public returns(bool){
        if(currentStatus=="[2, 1]" && action=="Ture-vvvv"){
            currentStatus="[3, 1]";
            return true;
        }
        else
            return false;
    }

    function Timeout-vvvv(String actionStr) public returns(bool){
        if(currentStatus=="[2, 1]" && action=="Ture-vvvv"){
            currentStatus="[3, 1]";
            return true;
        }
        else
            return false;
    }

    function Ture-cvcv(String actionStr) public returns(bool){
        if(currentStatus=="[2, 1]" && action=="Ture-vvvv"){
            currentStatus="[3, 1]";
            return true;
        }
        else
            return false;
    }

    function Timeout-cvcv(String actionStr) public returns(bool){
        if(currentStatus=="[2, 1]" && action=="Ture-vvvv"){
            currentStatus="[3, 1]";
            return true;
        }
        else
            return false;
    }

}
//BCMETH means Blockchain Match Ethereum 
pragma solidity ^0.4.24;

contract BCMETH {
    String currentStatus;
    constructor () public {
        currentStatus="[1, 1, 1, 1, 1]";
    }

    function (Bas0)(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1, 1, 1]" && action=="(Bas0)"){
            currentStatus="[2, 1, 1, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function (Sat0)(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1, 1, 1]" && action=="(Bas0)"){
            currentStatus="[2, 1, 1, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function (Vio0)(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1, 1, 1]" && action=="(Bas0)"){
            currentStatus="[2, 1, 1, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function (Sat1)(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1, 1, 1]" && action=="(Bas0)"){
            currentStatus="[2, 1, 1, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function (Vio1)(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1, 1, 1]" && action=="(Bas0)"){
            currentStatus="[2, 1, 1, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function (Exp1)(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1, 1, 1]" && action=="(Bas0)"){
            currentStatus="[2, 1, 1, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function (Sat2, Exp4)(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1, 1, 1]" && action=="(Bas0)"){
            currentStatus="[2, 1, 1, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function (Vio2, Exp4)(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1, 1, 1]" && action=="(Bas0)"){
            currentStatus="[2, 1, 1, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function (Exp2, Sat4)(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1, 1, 1]" && action=="(Bas0)"){
            currentStatus="[2, 1, 1, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function (Exp2, Vio4)(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1, 1, 1]" && action=="(Bas0)"){
            currentStatus="[2, 1, 1, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function (Exp2, Exp4)(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1, 1, 1]" && action=="(Bas0)"){
            currentStatus="[2, 1, 1, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function (Sat3)(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1, 1, 1]" && action=="(Bas0)"){
            currentStatus="[2, 1, 1, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function (Vio3)(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1, 1, 1]" && action=="(Bas0)"){
            currentStatus="[2, 1, 1, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function (Exp3)(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1, 1, 1]" && action=="(Bas0)"){
            currentStatus="[2, 1, 1, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function (Exp3)(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1, 1, 1]" && action=="(Bas0)"){
            currentStatus="[2, 1, 1, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function (Exp3)(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1, 1, 1]" && action=="(Bas0)"){
            currentStatus="[2, 1, 1, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function (Exp3)(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1, 1, 1]" && action=="(Bas0)"){
            currentStatus="[2, 1, 1, 1, 1]";
            return true;
        }
        else
            return false;
    }

}
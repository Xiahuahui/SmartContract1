//BCMETH means Blockchain Match Ethereum 
pragma solidity ^0.4.24;

contract BCMETH {
    String currentStatus;
    constructor () public {
        currentStatus="[1, 2, 1]";
    }

    function [["[['A', 'Satdeliver', 'Term2'], ['B', 'judge(B,4)', 'Term1Term1']]"]](String actionStr) public returns(bool){
        if(currentStatus=="[1, 2, 1]" && action=="[["[['A', 'Satdeliver', 'Term2'], ['B', 'judge(B,4)', 'Term1Term1']]"]]"){
            currentStatus="[2, 3, 1]";
            return true;
        }
        else
            return false;
    }

    function [["[['A', 'Viodeliver', 'Term2'], ['B', 'judge(B,4)', 'Term1Term1']]"]](String actionStr) public returns(bool){
        if(currentStatus=="[1, 2, 1]" && action=="[["[['A', 'Satdeliver', 'Term2'], ['B', 'judge(B,4)', 'Term1Term1']]"]]"){
            currentStatus="[2, 3, 1]";
            return true;
        }
        else
            return false;
    }

    function [["[['A', 'Satdeliver', 'Term2'], ['B', '!judge(B,4)', 'Term1Term1']]"]](String actionStr) public returns(bool){
        if(currentStatus=="[1, 2, 1]" && action=="[["[['A', 'Satdeliver', 'Term2'], ['B', 'judge(B,4)', 'Term1Term1']]"]]"){
            currentStatus="[2, 3, 1]";
            return true;
        }
        else
            return false;
    }

    function [["[['A', 'Viodeliver', 'Term2'], ['B', '!judge(B,4)', 'Term1Term1']]"]](String actionStr) public returns(bool){
        if(currentStatus=="[1, 2, 1]" && action=="[["[['A', 'Satdeliver', 'Term2'], ['B', 'judge(B,4)', 'Term1Term1']]"]]"){
            currentStatus="[2, 3, 1]";
            return true;
        }
        else
            return false;
    }

    function [["[['A', 'Satpay', 'Term1']]"]](String actionStr) public returns(bool){
        if(currentStatus=="[1, 2, 1]" && action=="[["[['A', 'Satdeliver', 'Term2'], ['B', 'judge(B,4)', 'Term1Term1']]"]]"){
            currentStatus="[2, 3, 1]";
            return true;
        }
        else
            return false;
    }

    function [["[['A', 'Viopay', 'Term1']]"]](String actionStr) public returns(bool){
        if(currentStatus=="[1, 2, 1]" && action=="[["[['A', 'Satdeliver', 'Term2'], ['B', 'judge(B,4)', 'Term1Term1']]"]]"){
            currentStatus="[2, 3, 1]";
            return true;
        }
        else
            return false;
    }

    function [["[['A', 'Satpay', 'Term1']]"]](String actionStr) public returns(bool){
        if(currentStatus=="[1, 2, 1]" && action=="[["[['A', 'Satdeliver', 'Term2'], ['B', 'judge(B,4)', 'Term1Term1']]"]]"){
            currentStatus="[2, 3, 1]";
            return true;
        }
        else
            return false;
    }

    function [["[['A', 'Viopay', 'Term1']]"]](String actionStr) public returns(bool){
        if(currentStatus=="[1, 2, 1]" && action=="[["[['A', 'Satdeliver', 'Term2'], ['B', 'judge(B,4)', 'Term1Term1']]"]]"){
            currentStatus="[2, 3, 1]";
            return true;
        }
        else
            return false;
    }

    function [["[['A', '', 'Term3']]"]](String actionStr) public returns(bool){
        if(currentStatus=="[1, 2, 1]" && action=="[["[['A', 'Satdeliver', 'Term2'], ['B', 'judge(B,4)', 'Term1Term1']]"]]"){
            currentStatus="[2, 3, 1]";
            return true;
        }
        else
            return false;
    }

    function [["[['A', '', 'Term3']]"]](String actionStr) public returns(bool){
        if(currentStatus=="[1, 2, 1]" && action=="[["[['A', 'Satdeliver', 'Term2'], ['B', 'judge(B,4)', 'Term1Term1']]"]]"){
            currentStatus="[2, 3, 1]";
            return true;
        }
        else
            return false;
    }

    function [["[['A', '', 'Term3']]"]](String actionStr) public returns(bool){
        if(currentStatus=="[1, 2, 1]" && action=="[["[['A', 'Satdeliver', 'Term2'], ['B', 'judge(B,4)', 'Term1Term1']]"]]"){
            currentStatus="[2, 3, 1]";
            return true;
        }
        else
            return false;
    }

    function [["[['A', '', 'Term3']]"]](String actionStr) public returns(bool){
        if(currentStatus=="[1, 2, 1]" && action=="[["[['A', 'Satdeliver', 'Term2'], ['B', 'judge(B,4)', 'Term1Term1']]"]]"){
            currentStatus="[2, 3, 1]";
            return true;
        }
        else
            return false;
    }

    function [["[['A', '', 'Term3']]"]](String actionStr) public returns(bool){
        if(currentStatus=="[1, 2, 1]" && action=="[["[['A', 'Satdeliver', 'Term2'], ['B', 'judge(B,4)', 'Term1Term1']]"]]"){
            currentStatus="[2, 3, 1]";
            return true;
        }
        else
            return false;
    }

    function [["[['A', '', 'Term3']]"]](String actionStr) public returns(bool){
        if(currentStatus=="[1, 2, 1]" && action=="[["[['A', 'Satdeliver', 'Term2'], ['B', 'judge(B,4)', 'Term1Term1']]"]]"){
            currentStatus="[2, 3, 1]";
            return true;
        }
        else
            return false;
    }

    function [["[['A', 'Sat', 'Term3']]"]](String actionStr) public returns(bool){
        if(currentStatus=="[1, 2, 1]" && action=="[["[['A', 'Satdeliver', 'Term2'], ['B', 'judge(B,4)', 'Term1Term1']]"]]"){
            currentStatus="[2, 3, 1]";
            return true;
        }
        else
            return false;
    }

    function [["[['A', 'Vio', 'Term3']]"]](String actionStr) public returns(bool){
        if(currentStatus=="[1, 2, 1]" && action=="[["[['A', 'Satdeliver', 'Term2'], ['B', 'judge(B,4)', 'Term1Term1']]"]]"){
            currentStatus="[2, 3, 1]";
            return true;
        }
        else
            return false;
    }

    function [["[['A', 'Sat', 'Term3']]"]](String actionStr) public returns(bool){
        if(currentStatus=="[1, 2, 1]" && action=="[["[['A', 'Satdeliver', 'Term2'], ['B', 'judge(B,4)', 'Term1Term1']]"]]"){
            currentStatus="[2, 3, 1]";
            return true;
        }
        else
            return false;
    }

    function [["[['A', 'Vio', 'Term3']]"]](String actionStr) public returns(bool){
        if(currentStatus=="[1, 2, 1]" && action=="[["[['A', 'Satdeliver', 'Term2'], ['B', 'judge(B,4)', 'Term1Term1']]"]]"){
            currentStatus="[2, 3, 1]";
            return true;
        }
        else
            return false;
    }

}
//BCMETH means Blockchain Match Ethereum 
pragma solidity ^0.4.24;

contract BCMETH {
    String currentStatus;
    constructor () public {
        currentStatus="[1, 1, 1]";
    }

    function Ture-sdf(String actionStr) public returns(bool){
        if(currentStatus=="[2, 1, 1]" && action=="Ture-sdf"){
            currentStatus="[3, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function Timeout-sdf(String actionStr) public returns(bool){
        if(currentStatus=="[2, 1, 1]" && action=="Ture-sdf"){
            currentStatus="[3, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function Timeout-03(String actionStr) public returns(bool){
        if(currentStatus=="[2, 1, 1]" && action=="Ture-sdf"){
            currentStatus="[3, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function Ture-dfd(String actionStr) public returns(bool){
        if(currentStatus=="[2, 1, 1]" && action=="Ture-sdf"){
            currentStatus="[3, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function Timeout-dfd(String actionStr) public returns(bool){
        if(currentStatus=="[2, 1, 1]" && action=="Ture-sdf"){
            currentStatus="[3, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function Ture-03(String actionStr) public returns(bool){
        if(currentStatus=="[2, 1, 1]" && action=="Ture-sdf"){
            currentStatus="[3, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function Ture-dfd(String actionStr) public returns(bool){
        if(currentStatus=="[2, 1, 1]" && action=="Ture-sdf"){
            currentStatus="[3, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function Timeout-dfd(String actionStr) public returns(bool){
        if(currentStatus=="[2, 1, 1]" && action=="Ture-sdf"){
            currentStatus="[3, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function Timeout-03(String actionStr) public returns(bool){
        if(currentStatus=="[2, 1, 1]" && action=="Ture-sdf"){
            currentStatus="[3, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function Ture-dfd(String actionStr) public returns(bool){
        if(currentStatus=="[2, 1, 1]" && action=="Ture-sdf"){
            currentStatus="[3, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function Timeout-dfd(String actionStr) public returns(bool){
        if(currentStatus=="[2, 1, 1]" && action=="Ture-sdf"){
            currentStatus="[3, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function Ture-sdf(String actionStr) public returns(bool){
        if(currentStatus=="[2, 1, 1]" && action=="Ture-sdf"){
            currentStatus="[3, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function Timeout-sdf(String actionStr) public returns(bool){
        if(currentStatus=="[2, 1, 1]" && action=="Ture-sdf"){
            currentStatus="[3, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function Ture-dfd(String actionStr) public returns(bool){
        if(currentStatus=="[2, 1, 1]" && action=="Ture-sdf"){
            currentStatus="[3, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function Timeout-dfd(String actionStr) public returns(bool){
        if(currentStatus=="[2, 1, 1]" && action=="Ture-sdf"){
            currentStatus="[3, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function Ture-sdf(String actionStr) public returns(bool){
        if(currentStatus=="[2, 1, 1]" && action=="Ture-sdf"){
            currentStatus="[3, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function Timeout-sdf(String actionStr) public returns(bool){
        if(currentStatus=="[2, 1, 1]" && action=="Ture-sdf"){
            currentStatus="[3, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function Timeout-03(String actionStr) public returns(bool){
        if(currentStatus=="[2, 1, 1]" && action=="Ture-sdf"){
            currentStatus="[3, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function Ture-sdf(String actionStr) public returns(bool){
        if(currentStatus=="[2, 1, 1]" && action=="Ture-sdf"){
            currentStatus="[3, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function Timeout-sdf(String actionStr) public returns(bool){
        if(currentStatus=="[2, 1, 1]" && action=="Ture-sdf"){
            currentStatus="[3, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function Ture-sdf(String actionStr) public returns(bool){
        if(currentStatus=="[2, 1, 1]" && action=="Ture-sdf"){
            currentStatus="[3, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function Timeout-sdf(String actionStr) public returns(bool){
        if(currentStatus=="[2, 1, 1]" && action=="Ture-sdf"){
            currentStatus="[3, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function Timeout-03(String actionStr) public returns(bool){
        if(currentStatus=="[2, 1, 1]" && action=="Ture-sdf"){
            currentStatus="[3, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function Ture-f(String actionStr) public returns(bool){
        if(currentStatus=="[2, 1, 1]" && action=="Ture-sdf"){
            currentStatus="[3, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function Timeout-f(String actionStr) public returns(bool){
        if(currentStatus=="[2, 1, 1]" && action=="Ture-sdf"){
            currentStatus="[3, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function Ture-dfd(String actionStr) public returns(bool){
        if(currentStatus=="[2, 1, 1]" && action=="Ture-sdf"){
            currentStatus="[3, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function Timeout-dfd(String actionStr) public returns(bool){
        if(currentStatus=="[2, 1, 1]" && action=="Ture-sdf"){
            currentStatus="[3, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function Ture-03(String actionStr) public returns(bool){
        if(currentStatus=="[2, 1, 1]" && action=="Ture-sdf"){
            currentStatus="[3, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function Ture-sdf(String actionStr) public returns(bool){
        if(currentStatus=="[2, 1, 1]" && action=="Ture-sdf"){
            currentStatus="[3, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function Timeout-sdf(String actionStr) public returns(bool){
        if(currentStatus=="[2, 1, 1]" && action=="Ture-sdf"){
            currentStatus="[3, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function Ture-03(String actionStr) public returns(bool){
        if(currentStatus=="[2, 1, 1]" && action=="Ture-sdf"){
            currentStatus="[3, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function Ture-dfd(String actionStr) public returns(bool){
        if(currentStatus=="[2, 1, 1]" && action=="Ture-sdf"){
            currentStatus="[3, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function Timeout-dfd(String actionStr) public returns(bool){
        if(currentStatus=="[2, 1, 1]" && action=="Ture-sdf"){
            currentStatus="[3, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function Timeout-03(String actionStr) public returns(bool){
        if(currentStatus=="[2, 1, 1]" && action=="Ture-sdf"){
            currentStatus="[3, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function Ture-sdf(String actionStr) public returns(bool){
        if(currentStatus=="[2, 1, 1]" && action=="Ture-sdf"){
            currentStatus="[3, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function Timeout-sdf(String actionStr) public returns(bool){
        if(currentStatus=="[2, 1, 1]" && action=="Ture-sdf"){
            currentStatus="[3, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function Timeout-03(String actionStr) public returns(bool){
        if(currentStatus=="[2, 1, 1]" && action=="Ture-sdf"){
            currentStatus="[3, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function Ture-dfd(String actionStr) public returns(bool){
        if(currentStatus=="[2, 1, 1]" && action=="Ture-sdf"){
            currentStatus="[3, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function Timeout-dfd(String actionStr) public returns(bool){
        if(currentStatus=="[2, 1, 1]" && action=="Ture-sdf"){
            currentStatus="[3, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function Ture-sdf(String actionStr) public returns(bool){
        if(currentStatus=="[2, 1, 1]" && action=="Ture-sdf"){
            currentStatus="[3, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function Timeout-sdf(String actionStr) public returns(bool){
        if(currentStatus=="[2, 1, 1]" && action=="Ture-sdf"){
            currentStatus="[3, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function Ture-sdf(String actionStr) public returns(bool){
        if(currentStatus=="[2, 1, 1]" && action=="Ture-sdf"){
            currentStatus="[3, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function Timeout-sdf(String actionStr) public returns(bool){
        if(currentStatus=="[2, 1, 1]" && action=="Ture-sdf"){
            currentStatus="[3, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function Ture-sdf(String actionStr) public returns(bool){
        if(currentStatus=="[2, 1, 1]" && action=="Ture-sdf"){
            currentStatus="[3, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function Timeout-sdf(String actionStr) public returns(bool){
        if(currentStatus=="[2, 1, 1]" && action=="Ture-sdf"){
            currentStatus="[3, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function Ture-sdf(String actionStr) public returns(bool){
        if(currentStatus=="[2, 1, 1]" && action=="Ture-sdf"){
            currentStatus="[3, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function Timeout-sdf(String actionStr) public returns(bool){
        if(currentStatus=="[2, 1, 1]" && action=="Ture-sdf"){
            currentStatus="[3, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function Timeout-03(String actionStr) public returns(bool){
        if(currentStatus=="[2, 1, 1]" && action=="Ture-sdf"){
            currentStatus="[3, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function Ture-sdf(String actionStr) public returns(bool){
        if(currentStatus=="[2, 1, 1]" && action=="Ture-sdf"){
            currentStatus="[3, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function Timeout-sdf(String actionStr) public returns(bool){
        if(currentStatus=="[2, 1, 1]" && action=="Ture-sdf"){
            currentStatus="[3, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function Timeout-03(String actionStr) public returns(bool){
        if(currentStatus=="[2, 1, 1]" && action=="Ture-sdf"){
            currentStatus="[3, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function Ture-dfd(String actionStr) public returns(bool){
        if(currentStatus=="[2, 1, 1]" && action=="Ture-sdf"){
            currentStatus="[3, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function Timeout-dfd(String actionStr) public returns(bool){
        if(currentStatus=="[2, 1, 1]" && action=="Ture-sdf"){
            currentStatus="[3, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function Ture-dfd(String actionStr) public returns(bool){
        if(currentStatus=="[2, 1, 1]" && action=="Ture-sdf"){
            currentStatus="[3, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function Timeout-dfd(String actionStr) public returns(bool){
        if(currentStatus=="[2, 1, 1]" && action=="Ture-sdf"){
            currentStatus="[3, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function Ture-f(String actionStr) public returns(bool){
        if(currentStatus=="[2, 1, 1]" && action=="Ture-sdf"){
            currentStatus="[3, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function Timeout-f(String actionStr) public returns(bool){
        if(currentStatus=="[2, 1, 1]" && action=="Ture-sdf"){
            currentStatus="[3, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function Ture-sdf(String actionStr) public returns(bool){
        if(currentStatus=="[2, 1, 1]" && action=="Ture-sdf"){
            currentStatus="[3, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function Timeout-sdf(String actionStr) public returns(bool){
        if(currentStatus=="[2, 1, 1]" && action=="Ture-sdf"){
            currentStatus="[3, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function Ture-f(String actionStr) public returns(bool){
        if(currentStatus=="[2, 1, 1]" && action=="Ture-sdf"){
            currentStatus="[3, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function Timeout-f(String actionStr) public returns(bool){
        if(currentStatus=="[2, 1, 1]" && action=="Ture-sdf"){
            currentStatus="[3, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function Ture-03(String actionStr) public returns(bool){
        if(currentStatus=="[2, 1, 1]" && action=="Ture-sdf"){
            currentStatus="[3, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function Ture-03(String actionStr) public returns(bool){
        if(currentStatus=="[2, 1, 1]" && action=="Ture-sdf"){
            currentStatus="[3, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function Ture-sdf(String actionStr) public returns(bool){
        if(currentStatus=="[2, 1, 1]" && action=="Ture-sdf"){
            currentStatus="[3, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function Timeout-sdf(String actionStr) public returns(bool){
        if(currentStatus=="[2, 1, 1]" && action=="Ture-sdf"){
            currentStatus="[3, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function Timeout-03(String actionStr) public returns(bool){
        if(currentStatus=="[2, 1, 1]" && action=="Ture-sdf"){
            currentStatus="[3, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function Timeout-03(String actionStr) public returns(bool){
        if(currentStatus=="[2, 1, 1]" && action=="Ture-sdf"){
            currentStatus="[3, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function Ture-sdf(String actionStr) public returns(bool){
        if(currentStatus=="[2, 1, 1]" && action=="Ture-sdf"){
            currentStatus="[3, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function Timeout-sdf(String actionStr) public returns(bool){
        if(currentStatus=="[2, 1, 1]" && action=="Ture-sdf"){
            currentStatus="[3, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function Ture-sdf(String actionStr) public returns(bool){
        if(currentStatus=="[2, 1, 1]" && action=="Ture-sdf"){
            currentStatus="[3, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function Timeout-sdf(String actionStr) public returns(bool){
        if(currentStatus=="[2, 1, 1]" && action=="Ture-sdf"){
            currentStatus="[3, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function Ture-sdf(String actionStr) public returns(bool){
        if(currentStatus=="[2, 1, 1]" && action=="Ture-sdf"){
            currentStatus="[3, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function Timeout-sdf(String actionStr) public returns(bool){
        if(currentStatus=="[2, 1, 1]" && action=="Ture-sdf"){
            currentStatus="[3, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function Ture-sdf(String actionStr) public returns(bool){
        if(currentStatus=="[2, 1, 1]" && action=="Ture-sdf"){
            currentStatus="[3, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function Timeout-sdf(String actionStr) public returns(bool){
        if(currentStatus=="[2, 1, 1]" && action=="Ture-sdf"){
            currentStatus="[3, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function Ture-sdf(String actionStr) public returns(bool){
        if(currentStatus=="[2, 1, 1]" && action=="Ture-sdf"){
            currentStatus="[3, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function Timeout-sdf(String actionStr) public returns(bool){
        if(currentStatus=="[2, 1, 1]" && action=="Ture-sdf"){
            currentStatus="[3, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function Ture-f(String actionStr) public returns(bool){
        if(currentStatus=="[2, 1, 1]" && action=="Ture-sdf"){
            currentStatus="[3, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function Timeout-f(String actionStr) public returns(bool){
        if(currentStatus=="[2, 1, 1]" && action=="Ture-sdf"){
            currentStatus="[3, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function Ture-f(String actionStr) public returns(bool){
        if(currentStatus=="[2, 1, 1]" && action=="Ture-sdf"){
            currentStatus="[3, 1, 1]";
            return true;
        }
        else
            return false;
    }

    function Timeout-f(String actionStr) public returns(bool){
        if(currentStatus=="[2, 1, 1]" && action=="Ture-sdf"){
            currentStatus="[3, 1, 1]";
            return true;
        }
        else
            return false;
    }

}
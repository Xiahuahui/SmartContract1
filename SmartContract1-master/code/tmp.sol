//BCMETH means Blockchain Match Ethereum 
pragma solidity ^0.4.24;

contract BCMETH {
    String currentStatus;
    constructor () public {
        currentStatus="[2, 1, 1]";
    }

    function (Term3C, Term1Asatpay)(String actionStr) public returns(bool){
        if(currentStatus=="[2, 1, 1]" && action=="(Term3C, Term1Asatpay)"){
            currentStatus="[3, 1, 4]";
            return true;
        }
        else
            return false;
    }

    function (Term3C, Term1Aviopay)(String actionStr) public returns(bool){
        if(currentStatus=="[2, 1, 1]" && action=="(Term3C, Term1Asatpay)"){
            currentStatus="[3, 1, 4]";
            return true;
        }
        else
            return false;
    }

    function (Term2C)(String actionStr) public returns(bool){
        if(currentStatus=="[2, 1, 1]" && action=="(Term3C, Term1Asatpay)"){
            currentStatus="[3, 1, 4]";
            return true;
        }
        else
            return false;
    }

    function (Term2Bjudge(A,pay))(String actionStr) public returns(bool){
        if(currentStatus=="[2, 1, 1]" && action=="(Term3C, Term1Asatpay)"){
            currentStatus="[3, 1, 4]";
            return true;
        }
        else
            return false;
    }

    function (Term2B!judge(A,pay))(String actionStr) public returns(bool){
        if(currentStatus=="[2, 1, 1]" && action=="(Term3C, Term1Asatpay)"){
            currentStatus="[3, 1, 4]";
            return true;
        }
        else
            return false;
    }

    function (Term2Bsatdeliver)(String actionStr) public returns(bool){
        if(currentStatus=="[2, 1, 1]" && action=="(Term3C, Term1Asatpay)"){
            currentStatus="[3, 1, 4]";
            return true;
        }
        else
            return false;
    }

    function (Term2Bviodeliver)(String actionStr) public returns(bool){
        if(currentStatus=="[2, 1, 1]" && action=="(Term3C, Term1Asatpay)"){
            currentStatus="[3, 1, 4]";
            return true;
        }
        else
            return false;
    }

    function (Term2Bsatdeliver)(String actionStr) public returns(bool){
        if(currentStatus=="[2, 1, 1]" && action=="(Term3C, Term1Asatpay)"){
            currentStatus="[3, 1, 4]";
            return true;
        }
        else
            return false;
    }

    function (Term2Bviodeliver)(String actionStr) public returns(bool){
        if(currentStatus=="[2, 1, 1]" && action=="(Term3C, Term1Asatpay)"){
            currentStatus="[3, 1, 4]";
            return true;
        }
        else
            return false;
    }

}
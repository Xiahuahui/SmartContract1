//BCMETH means Blockchain Match Ethereum 
pragma solidity ^0.4.24;

contract BCMETH {
    String currentStatus;
    constructor () public {
        currentStatus="[1]";
    }

    function Ture-grtgrt(String actionStr) public returns(bool){
        if(currentStatus=="[1]" && action=="Ture-grtgrt"){
            currentStatus="[2]";
            return true;
        }
        else
            return false;
    }

    function Timeout-grtgrt(String actionStr) public returns(bool){
        if(currentStatus=="[1]" && action=="Ture-grtgrt"){
            currentStatus="[2]";
            return true;
        }
        else
            return false;
    }

    function Ture-rtrte(String actionStr) public returns(bool){
        if(currentStatus=="[1]" && action=="Ture-grtgrt"){
            currentStatus="[2]";
            return true;
        }
        else
            return false;
    }

    function Timeout-rtrte(String actionStr) public returns(bool){
        if(currentStatus=="[1]" && action=="Ture-grtgrt"){
            currentStatus="[2]";
            return true;
        }
        else
            return false;
    }

}